<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <!-- Logo e icono de Instagram -->
        <a class="navbar-brand" asp-controller="UsuarioInfluencer" asp-action="ListarUsuariosInfluencer">
            <img src="~/imagenes/logo.png" alt="Logo" class="img-fluid" style="max-width: 150px;">
        </a>

        <!-- Botón de menú en móviles -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Menú de navegación -->
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-white" href="@Url.Action("ListarUsuariosInfluencer", "UsuarioInfluencer")">Usuarios Influencers</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="@Url.Action("ListarUsuariosEmpresa", "UsuarioEmpresa")">Usuarios Empresa</a>
                </li>
            </ul>
        </div>

        <!-- Botón de acción -->
        <div class="d-flex align-items-center">
            <form asp-controller="Sesion" asp-action="CerrarSesion" method="post" class="d-inline">
                <button type="submit" class="ms-3 d-flex align-items-center justify-content-center rounded-circle border border-success p-2 bg-transparent" style="width: 40px; height: 40px; border: none;">
                    <i class="fa fa-sign-out text-white"></i>
                </button>
            </form>
        </div>
    </div>
</nav>
